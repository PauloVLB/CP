#include <bits/stdc++.h>

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

using namespace std;

int a, b;

int main() {_
    cin >> a >> b;
    cout << (a+b <= 50 ? 'S' : 'N') << endl;
}
