#include <bits/stdc++.h>

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define endl '\n'

using namespace std;

map<string, string> m;

int main() {_   
    for(int i = 0; i < 3; i++) {
        string s, n; cin >> s >> n;
        m[s] = n;
    }

    int s;
    string i, f;
    cin >> s >> i >> f;
}